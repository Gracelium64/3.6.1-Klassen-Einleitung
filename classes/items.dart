class Items {
  final String itemId;
  final String itemImgUrl;

  Items(this.itemId, this.itemImgUrl);
}


class BtnSidebar extends Items {
    BtnSidebar(super.itemIdId, super.itemImgUrl);
}

class BtnSharingOptions extends Items {
    BtnSharingOptions(super.itemIdId, super.itemImgUrl);
}

class BtnPrize extends Items {
BtnPrize(super.itemIdId, super.itemImgUrl);
}

class BtnMain extends Items {
  BtnMain(super.itemIdId, super.itemImgUrl);
}