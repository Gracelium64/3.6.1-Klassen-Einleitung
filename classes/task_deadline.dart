class TaskDeadline {
  String taskCatagory;
  String taskDesctiption;
  DateTime date;
  DateTime time;
  bool isDone;

  TaskDeadline(this.taskCatagory,
               this.taskDesctiption, 
               this.date,
               this.time,
               this.isDone,
              );
}